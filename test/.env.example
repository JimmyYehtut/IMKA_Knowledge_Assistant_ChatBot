# run-questions-imka.py — the running IMKA backend and the account it logs in with.
# Copy to test/.env and fill in (test/.env is git-ignored).
IMKA_API_URL=http://localhost:8000
IMKA_EMAIL=
IMKA_PASSWORD=
